<Window x:Class="Desktop.TaskCreation"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Task creation" Height="225" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Content="_Name:" Target="{Binding ElementName=Name}" />
        <TextBox Grid.Row="1" Name="Name" />
        <Label Grid.Row="2" Content="_Description:" Target="{Binding ElementName=Description}" />
        <TextBox Grid.Row="3" Name="Description" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalAlignment="Stretch" />
        <Button Grid.Row="4" Content="Add" HorizontalAlignment="Right" IsDefault="True"
                Click="SaveTask" />
    </Grid>
</Window>